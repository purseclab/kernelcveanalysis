<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application android:label="EvilProvider" android:theme="@style/Theme.AppCompat.Light.NoActionBar">

        <provider
            android:name=".EvilDocumentProvider"
            android:authorities="com.example.evilprovider.documents"
            android:exported="true"
            android:grantUriPermissions="true"
            android:permission="android.permission.MANAGE_DOCUMENTS">
            <intent-filter>
                <action android:name="android.content.action.DOCUMENTS_PROVIDER" />
            </intent-filter>
        </provider>

    </application>

</manifest>
