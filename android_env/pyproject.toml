[project]
name = "android-env"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "typer>=0.20.0",
]

[tool.uv]
package = true

[tool.mypy]
mypy_path = "src"

[project.scripts]
aenv = "android_env.main:main"
