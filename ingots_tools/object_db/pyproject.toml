[project]
name = "object_db"
version = "0.1.0"
description = "Kernel object database models and extraction utilities"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "sqlalchemy>=2.0.45",
]

[tool.uv]
package = true
