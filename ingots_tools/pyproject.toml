[project]
name = "ingots-tools"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "android_env",
    "kexploit",
    "libadb",
    "scripts",
    "sandbox_env",
    "kexploit_utils",
]

[tool.uv.workspace]
members = [
  "android_env",
  "kexploit",
  "libadb",
  "scripts",
  "sandbox_env",
  "kexploit_utils",
]

[tool.uv.sources]
android_env = { workspace = true }
kexploit = { workspace = true }
libadb = { workspace = true }
scripts = { workspace = true }
sandbox_env = { workspace = true }
kexploit_utils = { workspace = true }
